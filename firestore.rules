rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas por defecto: solo lectura/escritura autenticada
    // Las API routes del backend manejan la autenticación
    match /{document=**} {
      allow read, write: if false; // Solo acceso desde API routes con autenticación
    }
  }
}

